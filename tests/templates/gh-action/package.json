{
  "name": "changeme",
  "version": "0.0.1",
  "description": "Package for github action",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "build": "ncc build lib/main.js -o dist",
    "format": "npx prettier --check .",
    "all": "npm run compile && npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@vercel/ncc": "^0.36.1",
    "prettier": "^2.8.7",
    "typescript": "^5.0.4"
  }
}
